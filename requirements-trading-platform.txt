# AI Trading Platform Requirements
# Install with: pip install -r requirements-trading-platform.txt

# Core dependencies
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9  # PostgreSQL driver for Railway deployment

# AI/ML
openai>=1.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
xgboost>=2.0.0  # Gradient boosting for ML predictions

# Advanced Math & Quant Libraries
PyPortfolioOpt>=1.5.0  # Portfolio optimization (Markowitz, Black-Litterman)
empyrical>=0.5.5  # Risk metrics (Sharpe, Sortino, VaR)
statsmodels>=0.14.0  # Statistical analysis, cointegration
hmmlearn>=0.3.0  # Hidden Markov Models for regime detection

# Technical Indicators
pandas-ta-openbb>=0.4.22  # 130+ technical indicators (OpenBB fork, Python 3.10 compatible)
# Note: Using OpenBB fork as official pandas-ta requires Python 3.12+
# TA-Lib requires C binaries, using pandas-ta as reliable alternative

# Fast Backtesting
vectorbt>=0.25.0  # 100x faster vectorized backtesting

# Visualization & Frontend
streamlit>=1.28.0
plotly>=5.17.0

# API client
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
